spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/frontrow
    username: frontrow
    password: frontrow
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092

frontrow:
  identity:
    dev-tokens-enabled: true
